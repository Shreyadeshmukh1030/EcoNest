[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Ensure API routes and dynamic pages are handled by Next.js Serverless Runtime
[functions]
  included_files = ["public/**"]
